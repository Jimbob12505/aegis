[project]
name = "aegis"
version = "0.0.1"
description = "Synthetic security log data generator (MVP)"
authors = [{name="Aegis Labs"}]
requires-python = ">=3.10"

[tool.setuptools]
package-dir = {"" = "src"}
packages = ["aegis"]

